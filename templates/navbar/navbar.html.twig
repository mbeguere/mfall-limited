<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
	<h5 class="my-0 mr-md-auto font-weight">MFALL LIMITED</h5>
	<nav class="my-2 my-md-0 mr-md-3">
		<a class="p-2 text-dark" href="{{ path('home_index') }}">Home</a>
		<a class="p-2 text-dark" href="{{ path('job_index') }}">Careers</a>
		{# <a class="p-2 text-dark" href="#">Enterprises</a>
																										<a class="p-2 text-dark" href="#">Support</a>
																										<a class="p-2 text-dark" href="#">Pricing</a> #}
	</nav>
	{% if app.user %}
		<a class="btn btn-warning" href="{{ path('app_logout') }}">Sign out</a>
	{% else %}
		{# <a class="btn btn-outline-info mr-1" href="#">Registration</a> #}
		<a class="btn btn-outline-primary mr-1" href="{{ path('app_login') }}">Sign in</a>
	{% endif %}
</div>
